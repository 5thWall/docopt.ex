defmodule Docopt.ParserTest do
  use ExUnit.Case

  alias Docopt.Parser

  # test "basic usage" do
  #   docopt = """
  #   Usage:
  #     foo
  #     foo -h | --help
  #   """

  #   Parser.parse(docopt) == {[:h, :help], []}
  # end
end
